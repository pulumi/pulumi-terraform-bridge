[env]
_.vfox-pulumi = { module_path = "" } # Sets GO_VERSION_MISE and PULUMI_VERSION_MISE

[tools]
# Runtimes
go = "{{ env.GO_VERSION_MISE }}"
# Executable tools
golangci-lint = '2.5.0'
pulumi = "{{ env.PULUMI_VERSION_MISE }}"
terraform = 'latest'

[plugins]
vfox-pulumi = "https://github.com/pulumi/vfox-pulumi"
